  <view class="row bg-gradual-black">
    <view class="col head" style="background-image: var(--gradualBlack)">
        <image src="{{userInfo.avatarUrl?userInfo.avatarUrl:'../../static/user status.png'}}"></image>
        <text wx:if="{{edit==true}}" bindtap="changeImage" class="weui-cell_link">选择图片</text>
    </view>
</view>


<view wx:if="{{edit==true}}">
    <view class="weui-toptips weui-toptips_warn" wx:if="{{showTopTips}}">错误提示</view>

    <!-- <view class="row form">
        <view class="col">
            <view class="col-3 label">昵称：</view>
            <view class="col-9 formItem">
                <input value="{{userInfo.nickName}}" bindinput="changeInputVal" data-type="nickName">
                </input>
            </view>
        </view>
    </view> -->

    <view class="cu-form-group">
      <view class="title">昵称</view>
      <input value="{{userInfo.nickName}}" bindinput="changeInputVal" data-type="nickName" type="text"></input>
      <!-- 去下划线 -->
	  </view>

    <!-- <view class="row form">
        <view class="col">
            <view class="col-3 label">性别：</view>
            <view class="col-9 formItem" bindtap="changeSex">
                <view wx:if="{{userInfo.gender == '1'}}">
                      <text  class="weui-cell_link">男生</text>
                </view>
                <view wx:elif="{{userInfo.gender == '2'}}" >
                     <text  class="weui-cell_link">女生</text>
                </view>
                <view wx:else >
                      <text  class="weui-cell_link">保密</text>
                </view>
            </view>
        </view>
    </view> -->

    <view class="cu-form-group">
      <view class="title">性别</view>
      <picker mode="selector" value="{{userInfo.gender}}" range="{{genterarray}}" bindchange="changeSex">
        <view class="picker textleft">
          {{genterarray[userInfo.gender]}}
        </view>
      </picker>
	  </view>

    <!-- <view class="row form">
        <view class="col">
            <view class="col-3 label">生日：</view>
            <view class="col-9 formItem">
                <picker mode="date"  value="{{userInfo.birthday}}" start="1970-01-01" bindchange="bindDateChange" bindtap="bindDateChange">
                    <view class="weui-input" wx:if="{{userInfo.birthday}}">
                        <text  class="weui-cell_link">{{userInfo.birthday}}</text>
                    </view>
                    <view class="weui-input" wx:else>
                        <text  class="weui-cell_link">请选择您的生日</text>
                    </view>
                </picker>
            </view>
        </view>
    </view> -->

    <view class="cu-form-group">
      <view class="title">生日</view>
      <picker mode="date" value="{{userInfo.birthday}}" bindchange="bindDateChange">
        <view class="picker textleft">
          {{userInfo.birthday}}
        </view>
      </picker>
	  </view>

    <!-- <view class="row form">
        <view class="col">
            <view class="col-3 label">地区：</view>
            <view class="col-9 formItem">
                <input placeholder="请输入地区" value="{{userInfo.city}}" bindinput="changeInputVal" data-type="city">
                </input>
            </view>
        </view>
    </view> -->

    <view class="cu-form-group">
		<view class="title">地址</view>
		<picker mode="region" bindchange="regionChange" value="{{region}}" custom-item="{{customItem}}">
			<view class="picker">
				{{region[0]}}，{{region[1]}}，{{region[2]}}
			</view>
		</picker>
	</view>

    <!-- <view class="row form">
        <view class="col">
            <view class="col-3 label">身份：</view>
            <view class="col-9 formItem">
                <input  placeholder="请定义您的身份" value="{{userInfo.role}}"bindinput="changeInputVal" data-type="role" >
                </input>
            </view>
        </view>
    </view> -->

      <view class="cu-form-group">
        <view class="title">身份</view>
        <picker mode="selector" value="{{userInfo.role}}" range="{{identyarray}}" bindchange="changeRole">
          <view class="picker textleft">
            {{identyarray[userInfo.role]}}
          </view>
        </picker>
	  </view>

    <!-- <view class="row form">
        <view class="col">
            <view class="col-3 label">性别：</view>
            <view class="col-9 formItem" bindtap="changeSex">
                <view wx:if="{{userInfo.gender == '1'}}">
                      <text  class="weui-cell_link">男生</text>
                </view>
                <view wx:elif="{{userInfo.gender == '2'}}" >
                     <text  class="weui-cell_link">女生</text>
                </view>
                <view wx:else >
                      <text  class="weui-cell_link">保密</text>
                </view>
            </view>
        </view>
    </view> -->

    <!-- <view class="row form">
        <view class="col">
            <view class="col-3 label">舞种：</view>
            <view class="col-8 formItem">
                <checkbox-group bindchange="chooseDanceType">
                    <label class="checkbox" wx:for="{{danceType}}" wx:key="*this">
                        <checkbox value="{{item.name}}" checked="{{item.checked}}"/>{{item.name}}
                    </label>
                </checkbox-group>
            </view>
        </view>
    </view> -->

  <view class="title row form" bindtap="showModal">舞种</view>
    <!-- <view class="row form">
        <view class="col">
            <view class="col-3 label">舞龄：</view>
            <view class="col-9 formItem">
                <radio-group class="radio-group" bindchange="chooseDanceAge">
                    <radio class="radio" wx:for-items="{{danceAge}}" wx:key="name" value="{{item.name}}" checked="{{item.checked}}">
                        <text>{{item.value}}</text>
                    </radio>
                </radio-group>
            </view>
        </view>
    </view> -->

    <view class="cu-form-group">
      <view class="title">舞龄</view>
      <picker mode="selector" value="{{userInfo.danceAge}}" range="{{agearray}}" bindchange="chooseDanceAge" bindtap="chooseDanceAge">
        <view class="picker textleft">
          {{agearray[userInfo.danceAge]}}
        </view>
      </picker>
	  </view>

    <view class="row form">
        <view class="col">
            <view class="col-3 label">个人介绍：</view>
            <view class="col-8 formItem">
                <textarea placeholder="介绍一下你自己吧~" bindinput="changeInputVal" value="{{userInfo.introduction}}" data-type="introduction">
                </textarea>
            </view>
        </view>
    </view>
    <view class="row form">
        <button style="width:80%" class="weui-btn" bindtap="saveUserInfo">确定</button>
    </view>

</view>
  <view wx:if="{{edit==false}}">
    <view class="cu-list bg-gradual-black menu {{menuBorder?'sm-border':''}} {{menuCard?'card-menu margin-top':''}}">
        <view class="cu-item bg-gradual-black">
            <view class="content">
                <text class=" cuIcon-friend lg text-blue"></text>
                <text class="text-grey">昵称：</text>
                <text>{{userInfo.nickName}}</text>
            </view>
        </view>
        <view class="cu-item bg-gradual-black">
            <view class="content">
                <text class=" cuIcon-friend lg text-blue"></text>
                <text class="text-grey">身份：</text>
                <text>{{userInfo.role}}</text>
            </view>
        </view>
        <view class="cu-item bg-gradual-black">
            <view class="content">
                <text class=" cuIcon-friend text-pink"></text>
                <text class="text-grey">性别：</text>
                <text wx:if="{{userInfo.gender=='1'}}">男生</text>
                <text wx:elif="{{userInfo.gender=='2'}}">女生</text>
                <text wx:else>保密</text>
            </view>
        </view>
        <view class="cu-item bg-gradual-black">
            <view class="content">
                <text class=" cuIcon-circlefill text-grey"></text>
                <text class="text-grey">地区：</text>
                <text>{{userInfo.city}}</text>
            </view>
        </view>
        <view style="opacity: 1" class="cu-item bg-gradual-black">
            <view class="content">
                <text class="cuIcon-tagfill text-green"></text>
                <text class="text-grey">舞种：</text>
                <block wx:for="{{userInfo.danceType}}">
                    <view wx:if="{{item.checked==true}}" class="{{item.class}}"role=""aria-label="">
                        {{item.name}}
                    </view>
                </block>
            </view>
        </view>
        <view style="opacity: 1" class="cu-item bg-gradual-black">
            <view class="content">
                <text class="cuIcon-rank text-red"></text>
                <text class="text-grey">舞龄：</text>
                <view wx:if="{{userInfo.danceAge=='lessOne'}}" class="cu-tag round bg-green light"role=""aria-label="">
                    少于一年
                </view>
                <view wx:elif="{{userInfo.danceAge=='oneToThree'}}" class="cu-tag round bg-green light"role=""aria-label="">
                    一至三年
                </view>
                <view wx:else class="cu-tag round bg-green light"role=""aria-label="">
                    三年以上
                </view>
            </view>
        </view>
        <view class="cu-item bg-gradual-black">
            <view class="content">
                <text class=" cuIcon-circlefill text-grey"></text>
                <text class="text-grey">签名：</text>
                <text>{{userInfo.introduction}}</text>
            </view>
        </view>
    </view>
    <view class="row form">
        <button  bindtap="editUserInfo" style="width:80%;margin: 0 0 0 10%" class="cu-btn block bg-yellow margin-tb-sm lg button-hover"role="button"aria-label=""aria-disabled="false">
            编辑
        </button>
    </view>
</view>
<view class="cu-modal show" wx:if="{{showModalFlag}}">
      <view class="cu-dialog">
          <view class="cu-bar bg-black justify-end">
              <view class="content">选择舞种</view>
              <view class="action" bindtap="hideModal">
                  <text class="cuIcon-close text-red"></text>
              </view>
          </view>
          <view class="padding-xl bg-black">
              <checkbox-group bindchange="chooseDanceType">
                  <label class="weui-cell weui-check__label" wx:for="{{danceType}}" wx:key="{{item.value}}">
                      <checkbox class="weui-check" value="{{item.name}}" checked="{{item.checked}}"/>

                      <view class="weui-cell__hd weui-check__hd_in-checkbox">
                          <icon class="weui-icon-checkbox_circle" type="circle" size="23" wx:if="{{!item.checked}}"></icon>
                          <icon class="weui-icon-checkbox_success" type="success" size="23" wx:if="{{item.checked}}"></icon>
                      </view>
                      <view class="weui-cell__bd">{{item.name}}</view>
                  </label>
              </checkbox-group>
          </view>
          <view class="cu-bar bg-black justify-end">
              <view class="action">
                  <button class="cu-btn bg-green margin-left" bindtap="hideModal">确定</button>
              </view>
          </view>
      </view>
</view>
