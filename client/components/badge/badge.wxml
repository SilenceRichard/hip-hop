<view wx:if="{{dot=='0'}}" class="weui-badge weui-badge_dot"></view>
<view wx:if="{{dot=='1'}}" class="weui-badge">
    <slot></slot>
    <!-- slot为插槽，可以放置任何HTML标签 -->
    {{num >=99?'99+':num}}
    <!-- 如果徽标数大于99，显示99+，否则显示真实数字 -->
</view>